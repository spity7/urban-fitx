version: '3.8'

services:
  client:
    build:
      context: ./client
    ports:
      - "3012:80"
    restart: always

  server:
    build:
      context: ./server
    ports:
      - "5013:5013"
    env_file:
      - ./server/.env
    restart: always
